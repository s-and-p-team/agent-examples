# newsroom-archive-tool — part of the newsroom example.
#
# Deployed the rossoctl way: a plain Deployment + Service, adopted by an
# AgentRuntime of type tool so the operator injects the AuthBridge sidecar.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: newsroom-archive-tool
  namespace: team2
---
apiVersion: v1
kind: Service
metadata:
  name: newsroom-archive-tool
  namespace: team2
  labels:
    app.kubernetes.io/name: newsroom-archive-tool
spec:
  ports:
    - name: http
      port: 8000
      protocol: TCP
      targetPort: 8000
  selector:
    app.kubernetes.io/name: newsroom-archive-tool
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: newsroom-archive-tool
  namespace: team2
  labels:
    app.kubernetes.io/name: newsroom-archive-tool
    app.kubernetes.io/component: tool
    app.kubernetes.io/part-of: newsroom
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: newsroom-archive-tool
  template:
    metadata:
      labels:
        app.kubernetes.io/name: newsroom-archive-tool
        app.kubernetes.io/part-of: newsroom
    spec:
      serviceAccountName: newsroom-archive-tool
      containers:
        - name: tool
          image: docker.io/library/newsroom_archive_tool:latest
          imagePullPolicy: IfNotPresent
          env:
            - { name: HOST, value: "0.0.0.0" }
            - { name: PORT, value: "8000" }
            - { name: UV_CACHE_DIR, value: "/app/.cache/uv" }
          ports:
            - { containerPort: 8000, name: http }
          readinessProbe:
            tcpSocket: { port: 8000 }
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 24
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits: { cpu: "1", memory: 1Gi }
          volumeMounts:
            - { name: cache, mountPath: /app/.cache }
      volumes:
        - name: cache
          emptyDir: {}
---
apiVersion: agent.rossoctl.dev/v1alpha1
kind: AgentRuntime
metadata:
  name: newsroom-archive-tool
  namespace: team2
spec:
  type: tool
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: newsroom-archive-tool
