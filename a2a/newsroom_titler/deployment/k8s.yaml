# newsroom-titler — part of the newsroom example.
#
# Deployed the rossoctl way: a plain Deployment + Service, adopted by an
# AgentRuntime so the operator injects the AuthBridge sidecar.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: newsroom-titler
  namespace: team2
---
apiVersion: v1
kind: Service
metadata:
  name: newsroom-titler
  namespace: team2
  labels:
    app.kubernetes.io/name: newsroom-titler
spec:
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 8000
  selector:
    app.kubernetes.io/name: newsroom-titler
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: newsroom-titler
  namespace: team2
  labels:
    app.kubernetes.io/name: newsroom-titler
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: newsroom
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: newsroom-titler
  template:
    metadata:
      labels:
        app.kubernetes.io/name: newsroom-titler
        app.kubernetes.io/part-of: newsroom
    spec:
      serviceAccountName: newsroom-titler
      containers:
        - name: agent
          image: docker.io/library/newsroom_titler:latest
          imagePullPolicy: IfNotPresent
          env:
            - { name: HOST, value: "0.0.0.0" }
            - { name: PORT, value: "8000" }
            - { name: UV_CACHE_DIR, value: "/app/.cache/uv" }
            - { name: AGENT_ENDPOINT, value: "http://newsroom-titler.team2.svc.cluster.local:8080/" }
            - { name: ARCHIVE_MCP_URL, value: "http://newsroom-archive-tool.team2.svc.cluster.local:8000/mcp" }
            - { name: LLM_API_BASE, value: "http://host.containers.internal:11434/v1" }
            - { name: LLM_MODEL, value: "qwen2.5:7b" }
            - { name: LLM_API_KEY, value: "ollama" }
          ports:
            - { containerPort: 8000, name: http }
          readinessProbe:
            tcpSocket: { port: 8000 }
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 24
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits: { cpu: "1", memory: 1Gi }
          volumeMounts:
            - { name: cache, mountPath: /app/.cache }
      volumes:
        - name: cache
          emptyDir: {}
---
apiVersion: agent.rossoctl.dev/v1alpha1
kind: AgentRuntime
metadata:
  name: newsroom-titler
  namespace: team2
spec:
  type: agent
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: newsroom-titler
